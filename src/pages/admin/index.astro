---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Admin Panel">
    <main class="min-h-screen bg-background">
        <div class="container mx-auto p-4 sm:p-6 lg:p-8 max-w-6xl">
            <!-- Header -->
            <div class="flex items-center gap-4 mb-6 sm:mb-8">
                <a
                    href="/"
                    class="p-2 hover:bg-accent rounded-lg touch-manipulation"
                    aria-label="Back to home"
                >
                    ← Back
                </a>
                <h1 class="text-2xl sm:text-3xl lg:text-4xl font-bold flex-1">
                    Admin Panel
                </h1>
            </div>

            <div class="grid gap-4 sm:gap-6 lg:grid-cols-2">
                <!-- Company Management -->
                <div
                    class="p-4 sm:p-6 border rounded-lg bg-card hover:shadow-lg transition-shadow"
                >
                    <h2
                        class="text-lg sm:text-xl font-semibold mb-3 flex items-center gap-2"
                    >
                        <span>🏢</span>
                        <span>Company Management</span>
                    </h2>
                    <p class="text-sm text-muted-foreground mb-4">
                        Manage company settings and employees
                    </p>
                    <button
                        class="w-full sm:w-auto px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 touch-manipulation"
                    >
                        Manage
                    </button>
                </div>

                <!-- Reports -->
                <div
                    class="p-4 sm:p-6 border rounded-lg bg-card hover:shadow-lg transition-shadow"
                >
                    <h2
                        class="text-lg sm:text-xl font-semibold mb-3 flex items-center gap-2"
                    >
                        <span>📈</span>
                        <span>Reports</span>
                    </h2>
                    <p class="text-sm text-muted-foreground mb-4">
                        View company reports and analytics
                    </p>
                    <button
                        class="w-full sm:w-auto px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 touch-manipulation"
                    >
                        View Reports
                    </button>
                </div>

                <!-- Employees -->
                <div
                    class="p-4 sm:p-6 border rounded-lg bg-card hover:shadow-lg transition-shadow"
                >
                    <h2
                        class="text-lg sm:text-xl font-semibold mb-3 flex items-center gap-2"
                    >
                        <span>👥</span>
                        <span>Employees</span>
                    </h2>
                    <p class="text-sm text-muted-foreground mb-4">
                        Manage employee accounts and permissions
                    </p>
                    <button
                        class="w-full sm:w-auto px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 touch-manipulation"
                    >
                        Manage Team
                    </button>
                </div>

                <!-- Settings -->
                <div
                    class="p-4 sm:p-6 border rounded-lg bg-card hover:shadow-lg transition-shadow"
                >
                    <h2
                        class="text-lg sm:text-xl font-semibold mb-3 flex items-center gap-2"
                    >
                        <span>⚙️</span>
                        <span>Settings</span>
                    </h2>
                    <p class="text-sm text-muted-foreground mb-4">
                        Configure system preferences
                    </p>
                    <button
                        class="w-full sm:w-auto px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 touch-manipulation"
                    >
                        Settings
                    </button>
                </div>
            </div>
        </div>
    </main>
</Layout>
